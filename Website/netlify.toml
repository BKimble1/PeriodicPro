# Elemora — elemora.idlery.com
#
# Plain static files with one substitution: the Apple Team ID is injected into
# the app-association file at deploy time from an environment variable, so no
# signing identifier is ever committed.

[build]
  command = "bash scripts/build.sh"
  publish = "site"

[build.environment]
  NODE_VERSION = "22"
